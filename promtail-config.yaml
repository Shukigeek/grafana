server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push


# directly from containers
scrape_configs:
  - job_name: drone_containers
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s
    relabel_configs:
      - source_labels: [__meta_docker_container_name]
        target_label: container


#from logs files
#scrape_configs:
#  - job_name: drone_logs
#    static_configs:
#      - targets:
#          - localhost
#        labels:
#          job: drone
#          __path__: /path/on/host/logs/*.log